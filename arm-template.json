{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "extensionResourceName": {
      "type": "string",
      "metadata": {
        "description": "The name of the extension."
      }
    },
    "clusterResourceName": {
      "type": "String",
      "metadata": {
        "description": "The name of the Managed Cluster resource."
      }
    }
  },
  "variables": {
    "plan-name": "alluxio-operator",
    "plan-publisher": "alluxio1770783070184",
    "plan-offerID": "alluxio_operator",
    "releaseTrain": "stable",
    "clusterExtensionTypeName": "alluxio.alluxio-operator-extensions"
  },
  "resources": [
    {
      "type": "Microsoft.KubernetesConfiguration/extensions",
      "apiVersion": "2023-05-01",
      "name": "[parameters('extensionResourceName')]",
      "location": "[resourceGroup().location]",
      "scope": "[concat('Microsoft.ContainerService/managedClusters/', parameters('clusterResourceName'))]",
      "plan": {
        "name": "[variables('plan-name')]",
        "publisher": "[variables('plan-publisher')]",
        "product": "[variables('plan-offerID')]"
      },
      "properties": {
        "extensionType": "[variables('clusterExtensionTypeName')]",
        "releaseTrain": "[variables('releaseTrain')]",
        "autoUpgradeMinorVersion": true,
        "configurationSettings": {
        },
        "configurationProtectedSettings": {}
      }
    }
  ]
}